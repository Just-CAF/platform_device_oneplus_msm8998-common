dontaudit hal_audio_default sysfs:dir { open read };
set_prop(hal_audio_default, diag_prop)

allow hal_audio_default bluetooth_audio_so:file { rx_file_perms };

allow hal_audio_default sysfs_soc:dir { r_dir_perms };
allow hal_audio_default sysfs_soc:file { r_file_perms };
allow hal_audio_default sysfs_virtual:dir { search };
